import 'package:flutter/material.dart';

TextStyle ralewayBold({required double tSize, required Color tColor}) {
  return TextStyle(
    fontFamily: 'Raleway',
    fontWeight: FontWeight.w700,
    fontSize: tSize,
    color: tColor,
    height: 1.3,
  );
}

TextStyle ralewayMedium({required double tSize, required Color tColor}) {
  return TextStyle(
    fontFamily: 'Raleway',
    fontWeight: FontWeight.w500,
    fontSize: tSize,
    color: tColor,
    height: 1.3,
  );
}

TextStyle urbanistSemiBold({required double tSize, required Color tColor}) {
  return TextStyle(
    fontFamily: 'Urbanist',
    fontWeight: FontWeight.w600,
    fontSize: tSize,
    color: tColor,
    height: 1.3,
  );
}

TextStyle urbanistMedium({required double tSize, required Color tColor}) {
  return TextStyle(
    fontFamily: 'Urbanist',
    fontWeight: FontWeight.w500,
    fontSize: tSize,
    color: tColor,
    height: 1.3,
  );
}

TextStyle urbanistBold({required double tSize, required Color tColor}) {
  return TextStyle(
    fontFamily: 'Urbanist',
    fontWeight: FontWeight.w700,
    fontSize: tSize,
    color: tColor,
    height: 1.3,
  );
}

TextStyle urbanistRegular({required double tSize, required Color tColor}) {
  return TextStyle(
    fontFamily: 'Urbanist',
    fontWeight: FontWeight.w400,
    fontSize: tSize,
    color: tColor,
    height: 1.3,
  );
}
